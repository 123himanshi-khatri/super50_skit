// Abstract base class — defines the interface for all problems
public abstract class ProblemSolver {
    // Abstract method (must be overridden)
    public abstract void inputArray();
    public abstract void solve();
}
